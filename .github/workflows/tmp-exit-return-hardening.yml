name: Temporary Exit Return Hardening
on:
  push:
    branches:
      - fix/employee-request-workflow-hardening
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/employee-request-workflow-hardening
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply exit return hardening
        run: python scripts/tmp-exit-return-hardening.py
      - name: Verify focused scope and build
        run: |
          npm ci
          node --test workers/exit-return-request-hardening.test.mjs workers/resignation-request-hardening.test.mjs workers/request-form-hardening.test.mjs workers/attendance-correction-request-hardening.test.mjs workers/permission-lifecycle-hardening.test.mjs
          npm run build
          git diff --check
      - name: Commit final patch and remove temporary runner
        run: |
          git config user.name "nawafoly"
          git config user.email "114729674+nawafoly@users.noreply.github.com"
          rm -f scripts/tmp-exit-return-hardening.py .github/workflows/tmp-exit-return-hardening.yml
          git add workers/core/repositories/employee-requests-legacy.js workers/exit-return-request-hardening.test.mjs scripts/tmp-exit-return-hardening.py .github/workflows/tmp-exit-return-hardening.yml
          git commit -m "fix(hr): harden exit return actual lifecycle"
          git push
